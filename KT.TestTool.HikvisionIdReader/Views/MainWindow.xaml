<Window x:Class="KT.TestTool.HikvisionIdReader.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/" xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded" >
            <i:CallMethodAction MethodName="ViewLoaded" TargetObject="{Binding}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <!--<ContentControl prism:RegionManager.RegionName="ContentRegion" />-->
        <StackPanel Orientation="Vertical">
            <Button Width="100" Height="50" Content="查询设备并登录" Command="{Binding SearchDeviceCommand}"/>
            <Button Width="100" Height="50" Content="读取身份证信息" Command="{Binding ReadIdCardCommand}"/>

            <TextBlock TextWrapping="Wrap" Text="{Binding Devices}" />
        </StackPanel>

    </Grid>
</Window>
