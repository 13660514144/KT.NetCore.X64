<pu:WindowX x:Class="KT.Elevator.Unit.Dispatch.ClientApp.Views.MainWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:pu="clr-namespace:Panuon.UI.Silver;assembly=Panuon.UI.Silver"
            mc:Ignorable="d"
            Title="MainWindow"
            Height="600" 
            Width="400">
    <Grid>
        <Viewbox Stretch="Fill">
            <Grid Height="1000" Width="600">
                <ContentPresenter Content="{Binding ShowControl}"/>
                <ContentPresenter Content="{Binding WelcomeControl}"/>
                <ContentPresenter Content="{Binding WarnTipControl}" VerticalAlignment="Bottom" Margin="60,0,60,100"/>
            </Grid>
        </Viewbox>
    </Grid>

</pu:WindowX>
